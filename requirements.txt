Flask>=3.0
PyJWT>=2.7

# Only needed if you point DATABASE_URL at a cloud Postgres database
# (Supabase, Neon, Render, Railway, AWS RDS, etc). Not required for the
# default local SQLite setup.
psycopg2-binary>=2.9
